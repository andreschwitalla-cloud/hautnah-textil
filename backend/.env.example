# App
SECRET_KEY=           # mind. 32 Zeichen, z.B. per: python -c "import secrets; print(secrets.token_urlsafe(48))"
ADMIN_PASSWORD_HASH=  # bcrypt-Hash, generieren per: python -c "import bcrypt; print(bcrypt.hashpw(b'passwort', bcrypt.gensalt(12)).decode())"

# E-Mail (Resend)
RESEND_API_KEY=       # API Key aus resend.com
RESEND_TO=            # Empfänger-Adresse für Kontaktformular-Benachrichtigungen
